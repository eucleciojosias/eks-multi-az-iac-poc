[project]
name = "movies-api-scripts"
version = "0.1.0"
description = "Standalone utility scripts for the movies API"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
